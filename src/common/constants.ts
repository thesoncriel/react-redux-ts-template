import appConfig from './app.config';

/**
 * 정적 이미지의 기본 경로.
 */
export const URL_BASE_STATIC_IMAGE = appConfig.staticVerUrl + '/images/';

/**
 * 인덱스 (메인) 페이지에서 쓰이는 정적 이미지의 기본 경로.
 */
export const URL_BASE_STATIC_IMAGE_INDEX = URL_BASE_STATIC_IMAGE + 'index/';
