/**
 * 프로젝트 내 공용으로 쓰이는 각종 여백 수치를 정의한다.
 *
 * 접두어별 역할
 * - PADDING_{name} - 4방향 안쪽 여백
 * - PADDING_{TOP|RIGHT|LEFT|BOTTOM}_{name} - 특정 방향 안쪽 여백
 * - SIDE_PADDING_{name} - 좌/우 안쪽 여백
 * - MARGIN_{name} - 4방향 바깥 여백
 * - MARGIN_{TOP|RIGHT|LEFT|BOTTOM}_{name} - 특정 방향 바 여백
 * - SIDE_MARGIN_{name} - 좌/우 바깥 여백깥
 */

export const SIDE_PADDING = 40;
export const PADDING_TOP_HEADER = 10;
export const MARGIN_SECTION = 20;
